#ifndef CONSTANTS_HPP
#define CONSTANTS_HPP

#include <SDL.h>

inline constexpr int GB_SCREEN_WIDTH = 160;
inline constexpr int GB_SCREEN_HEIGHT = 144;
inline constexpr int START_SCREEN_WIDTH = 640;
inline constexpr int START_SCREEN_HEIGHT = 480;

inline constexpr int FONT_SIZE = 14;

inline constexpr int KEYBOARD_ID = -1;
inline constexpr int NO_INPUT = -2;

#endif // CONSTANTS_HPP